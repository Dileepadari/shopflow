# Runtime dependencies for the consumers, producers, Producer API and cluster_init.
# The Chaos Service adds the Docker SDK on top of these - see chaos_service/requirements.txt.
pika==1.4.2
fastapi==0.141.1
uvicorn[standard]==0.52.1
httpx==0.28.1
python-dotenv==1.0.1
